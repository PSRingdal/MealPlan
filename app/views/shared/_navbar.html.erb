<nav class="navbar navbar-expand-sm">
  <div class="container-fluid">
      <% if user_signed_in? %>
        <div class="d-flex align-items-center gap-3">
          <i class="fa-solid fa-utensils"></i>
          <h1>Easy Plate</h1>
          <form data-search-target="form" data-action="submit->search#submit">
            <div class="form-group d-flex">
              <input type="text" class="form-control" data-search-target="inputField" placeholder="Search dishes">
            </div>
          </form>
        </div>

        <div class="d-flex align-items-center gap-3">
          <div data-category-target="selectedMeals">
            <button class="btn btn-dark"> Your plan <span data-category-target="selectedCount">0</span></button>
          </div>
          <i class="fa-solid fa-gear"></i>
        </div>

      <% else %>
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        </ul>
      <% end %>
  </div>
</nav>

<div class="container ">
  <h1>Build a meal plan</h1>

  <div class="search-recipe" data-controller="search category">
    <form data-search-target="form" data-action="submit->search#submit">
      <div class="form-group d-flex">
        <input type="text" class="form-control" data-search-target="inputField">
        <button type="submit" class="btn btn-secondary">Search</button>
      </div>
    </form>

    <div  data-search-target="mealCards" data-category-target="mealCards" >
      <% @categories.each do |category| %>
        <div
          data-category-target="category"
          data-category-name="<%= category %>">
        </div>
      <% end %>
    </div>
      <div class="selected-meals" data-category-target="selectedMeals">
        <button class="btn btn-success bottom-btn">Build plan</button>
        <p>Selected <span data-category-target="selectedCount">0</span></p>
      </div>
  </div>



</div>
